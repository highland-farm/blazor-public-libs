{
  "private": true,
  "scripts": {
    "build": "tsc && esbuild BarcodeScanner.ts --bundle --minify --sourcemap --target=es2015 --platform=browser --format=esm --outfile=../wwwroot/tslib.esm.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.11.14",
    "typescript": "^4.2.4"
  }
}
